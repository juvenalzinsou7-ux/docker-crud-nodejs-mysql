# Variables d'environnement de l'application.
# Copiez ce fichier en « .env » et adaptez les valeurs si nécessaire.
#
# En local : DB_HOST=localhost
# Dans Docker : DB_HOST = nom du conteneur MySQL (ex : db)

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=inventory
PORT=3000
